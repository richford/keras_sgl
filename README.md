# Sparse Group Lasso implementation in `keras`

This repository proposes a `keras` implementation of Sparse Group Lasso models for
logistic regression and linear regression using the popular `keras` framework.
